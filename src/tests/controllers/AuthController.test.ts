const { describe, it } = require("@jest/globals");
const request = require("supertest");
 
// Auth controller test script 
// full branches coverage 
// incomplete fields
// existing user
// inexisting user
describe("Test authController", () => {
  it("Create account with incomplete fields", async () => {
  });
});
